
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Platzi Master Test</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-wEmeIV1mKuiNpC+IOBjI7aAzPcEZeedi5yW5f2yOq55WWLwNGmvvx4Um1vskeMj0" crossorigin="anonymous">
</head>
<body>
    <nav class="navbar navbar-dark bg-dark mb-4">
        <a href="/" class="navbar-brand">Platzi Master Test</a>
    </nav>
   
    <div class="container">
        <div class="row">
            <div class="col-md-5">
                <div class="card">
                    <div class="card-body">
                        <form action="/editProduct/<%= product._id %>" method="POST">
                            <div class="form-group mb-2">
                                <input type="text" name="name" placeholder="Ingresa el nombre del producto" class="form-control mb-2" value="<%= product.name %>">
                                <input type="text" name="price" placeholder="Ingresa el precio del producto" class="form-control mb-2" value="<%= product.price %>">
                                <input type="text" name="description" placeholder="Ingresa la descripción del producto" class="form-control mb-2" value="<%= product.description %>">
                                <select name="categoryId" class="form-control mb-2">
                                    <option value=”0”>Seleccionar</option>
                                    <% for(var i = 0; i < categories.length; i++ ){ %>
                                        <option value=”<%= categories[i]._id %>” <%= categories[i]._id == product.categoryId ? "selected" : "" %>><%= categories[i].name %></option>
                                    <% } %>
                                </select>
                            </div>
                            <button type="submit" class="btn btn-primary">Guardar</button>
                        </form>
                    </div>
                </div>
            </div>

           
            </div>

        </div>
       
    </div>
</body>
</html>